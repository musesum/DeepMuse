MuMenu
    Touch
        fix double tap on separate nodes
    branch

    node
        tog without child
    home
        leaf icon in retracted home
    leaf
        ipad pencel vxy center

        hide super branches while editing
        emboss thumb with icon
        special title
        touch long press lift no change
        vx, vy, hx, hy, vxy -> xy

    sync devices

Sky
    touche
        multitouch dispatch menu + canvas
    bugs
        camera
        Autorotate autofit view
    features
        sync devices
        resize/rotate canvas
        resize texture

Tr3
    get a(b 1) as find("a.b")
    a.*.{ decorate } for sky
    test connect by name `menu.model :> shader.model`
    snapshot abstract def with updated state

    later
        replace Tr3ValScalar with Tr3Exprs
        update tr3val min


